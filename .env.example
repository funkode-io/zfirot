# Copy this file to `.env` and fill in your token. `.env` is gitignored.
#
# A fine-grained GitHub Personal Access Token with read access to the target
# repository's Issues, Pull requests, and Contents. The app reads this at
# startup to load the board.
GITHUB_TOKEN=
